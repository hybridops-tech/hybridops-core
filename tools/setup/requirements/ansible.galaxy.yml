collections:
  - name: hybridops.common
    version: "0.1.6"

  - name: hybridops.helper
    version: "0.1.4"

  - name: hybridops.app
    version: "0.1.4"

  - name: ansible.posix
    version: "1.6.2"

  # EVE-NG module dependency
  - name: community.mysql

  # PostgreSQL modules dependency (Ansible >= 2.10)
  - name: community.postgresql
  - name: community.general

roles:
  # postgresql_service role dependency
  - name: geerlingguy.postgresql
