# Create or update a standby DR volume from a Longhorn backup.

kubeconfig_path: "/path/to/cluster.kubeconfig"
longhorn_namespace: "longhorn-system"
operation_mode: "standby"
backup_name: "moodle-cutover-20260316t1622z-backup"
restore_volume_name: "academy-moodle-dr-standby"
restore_number_of_replicas: 1
wait_restore_complete: true
