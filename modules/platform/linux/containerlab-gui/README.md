# Linux Containerlab GUI

Runs pinned official Containerlab API and web GUI containers on an existing
Containerlab host. The web endpoint is published on host loopback and is reached
directly on the host or through the blueprint access tunnel.

The login user must exist on the target Linux host. Password management is
disabled unless explicitly requested.
