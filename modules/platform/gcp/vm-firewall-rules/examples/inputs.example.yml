name_prefix: platform
context_id: dev
network: default
rules:
  - name_suffix: allow-ssh
    description: SSH from the operator network
    direction: INGRESS
    source_ranges:
      - 203.0.113.10/32
    target_tags:
      - allow-ssh
    allow:
      - protocol: tcp
        ports:
          - "22"
