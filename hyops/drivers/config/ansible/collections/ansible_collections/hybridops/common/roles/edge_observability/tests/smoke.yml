---
- name: Smoke test edge_observability role
  hosts: edge
  become: true
  gather_facts: true

  collections:
    - hybridops.common

  roles:
    - role: edge_observability
      vars:
        edge_obs_objstore_config: |
          type: GCS
          config:
            bucket: example
            service_account: |-
              { ... }
        edge_obs_hashring_endpoints:
          - "edge-1.internal:10907"
