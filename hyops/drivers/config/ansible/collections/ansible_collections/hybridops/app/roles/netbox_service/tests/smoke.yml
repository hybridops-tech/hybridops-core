---
# purpose: Smoke test for netbox_service role
# adr: ADR-0606-ansible-collections-release-process
# maintainer: HybridOps.Tech

- name: NetBox service smoke test
  hosts: cicd_test
  become: true
  vars: {}
  tasks:
    - name: Apply netbox_service (smoke-only)
      ansible.builtin.include_role:
        name: hybridops.app.netbox_service
